export enum DigitalInputPlusStatus {
	Cerrado = 0,
	Abierto = 1,
}

export enum ValvePlusModes {
	Manual = 0,
	Auto = 1,
}

export enum ValvePlusStatus {
	Cerrada = 0,
	Abierta = 1,
}

export enum PumpPluStatus {
	Cerrada = 0,
	Abierta = 1,
}

export enum PumpPlusModes {
	Manual = 0,
	Auto = 1,
}

export enum FertilizerPlusStatus {
	Cerrado = 0,
	Abierto = 1,
}

export enum FertilizerPlusModes {
	Manual = 0,
	Auto = 1,
}

export enum MixerPlusStatus {
	Cerrado = 0,
	Abierto = 1,
}

export enum MixerPlusModes {
	Manual = 0,
	Auto = 1,
}
